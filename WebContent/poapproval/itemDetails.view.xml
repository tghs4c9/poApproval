<core:View xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc" xmlns:form="sap.ui.layout.form" xmlns="sap.m"
		controllerName="poapproval.itemDetails" xmlns:html="http://www.w3.org/1999/xhtml">
	
	<Page title="PO Line Item Details">
		
		<content>
	
	<ObjectHeader 
    			    id="header" title="{SupplierName}" responsive="true"
					number="{parts: [ {path: 'GrossAmount'}, {path: 'CurrencyCode'}],
                        type : 'sap.ui.model.type.Currency',
                        formatOptions: { showMeasure: false }}" 
    			    numberUnit="{CurrencyCode}"
    			    intro="{parts:[{path: 'i18n>xfld.orderedBy'}, {path: 'OrderedByName'}], formatter: 'jQuery.sap.formatMessage'}">
    			    <statuses>
    				    <ObjectStatus id="status" 
    				    	text="{path: 'ChangedAt', type: 'sap.ui.model.type.Date',
									formatOptions: { style: 'medium',
													strictParsing: true,
													relative: true }}" 
    				    	title="{/#PurchaseOrder/ChangedAt/@sap:label}"
    				    />
    			    </statuses>
    		    </ObjectHeader>
	
	<Input id ="po" type="number" value = "{po>/po}" ></Input>
	
	<List
		id="idList"
		 
		items="{items>/PurchaseOrderItem/results}"
		  >
		<items>
			<ObjectListItem
			   
				title="{items>Material}"
				intro="{items>POItem}"
				number = "{items>NetValue}"
				numberUnit = "{items>Currency}"
				/>
		</items>
	</List>
			</content>
		<footer>
			<Toolbar id="otbFooter">
				<ToolbarSpacer />
				<Button type="Accept" text="Approve" press="approve">
					<layoutData>
						<OverflowToolbarLayoutData
							moveToOverflow="false" />
					</layoutData>
				</Button>
				<Button type="Reject" text="Reject" press="reject">
					<layoutData>
						<OverflowToolbarLayoutData
							moveToOverflow="false" />
					</layoutData>
				</Button>
				
				<OverflowToolbarButton press="logout" text="Logout"  icon="sap-icon://log" />

			</Toolbar>
		
	</footer>
	
		</Page>
		
</core:View>